frontEnd(cliente){
	Toda caracteristica o funcionalidad que puede reproducir el navegador sin depender de un servidor (UI / UX / Scripts).

	Lenguajes:
		html	ESTRUCTURA
		css		DISEÑO	
		js 		FUNCIONALIDAD
}
backEnd(servidor){
	Toda consulta de datos o funcionalidad que debe ser resuelta por el servidor para llevarse a cabo y luego reflejarse en el frontend.

	Lenguajes:
		sql		BASE DATOS
		php		WEB DINAMICAS
		java	APLICACIONES WEB
}
fullStack(cliente, servidor){
	Organizacion y liderazgo de equipos de trabajo para el desarrollo de una aplicacion web. Optimizacion y depuracion de la misma

	Lenguajes:
		Todos los anteriores
}