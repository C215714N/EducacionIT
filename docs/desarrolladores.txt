Front-end(cliente){
	La interfaz que se carga en el navegador, Desarrollo del lado del Cliente (lo que se ve)
	Lenguajes:
		HTML	Estructura
		CSS		Diseño
		JS		Funcionalidad
}
Back-end(servidor){
	la manipulacion de los datos, Desarrollo del lado del servidor (lo que no se ve)
	Lenguajes:
		SQL		Base Datos
		PHP		Web Dinamicas
		JAVA	Aplicaciones Web
}
Fullstack(ambos){
	Todos los anteriores, generalmente los desarrolladores fullstack se encargan de la organizacion de proyectos.
}